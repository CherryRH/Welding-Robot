一、核心问题：为什么还是绕不过去？

你现在的策略本质是：

“沿障碍切线方向滑动 + 被目标吸引”

但问题在于：

❗ 本质缺陷：局部极小值 + 错误拓扑选择

面对这种情况：

Start →→→→→  | 障碍墙 |   Goal

APF会出现：

引力 → 指向墙
排斥力 ← 推开
切向 → 沿墙滑

👉 但“滑向哪一边”是局部决策，没有全局信息

结果就是：

可能来回横跳
或贴着墙走但永远绕不到终点方向
或 stuck 在某个“平衡轨道”
二、你代码里的几个关键问题（重点）
1️⃣ 切向方向选择逻辑不稳定（最大问题）
Vector3 tangent = Vector3.Dot(tangent1, F_att) > Vector3.Dot(tangent2, F_att)
    ? tangent1 : tangent2;

👉 问题：

这是“瞬时最优”
每一帧可能改变方向
导致：
抖动
来回横跳
无法稳定绕行
✅ 必须改成：固定绕行方向（wall-following side）

👉 核心思想：

一旦进入“贴墙状态”，就锁定一个方向（上/左/右）

✔ 修改方案：
// 类成员变量（必须跨迭代保存）
private int wallFollowSign = 0; // +1 or -1

// 初始化（第一次接触障碍）
if (wallFollowSign == 0)
{
    wallFollowSign = Vector3.Dot(tangent1, F_att) > 0 ? 1 : -1;
}

// 固定方向
Vector3 tangent = (wallFollowSign > 0 ? tangent1 : tangent2).normalized;

👉 这个改动非常关键，会直接解决“绕不过去”的大部分问题。

2️⃣ 切向力没有距离衰减（会压制引力）

你现在：

F += tangent.normalized * wTan + normal * wRep;

👉 问题：

不管离障碍多远，切向力都一样强
导致：
一直沿墙走
无法“离开墙体”
✔ 改成经典APF形式：
float repScale = (1f / envDist - 1f / influenceRadius);
repScale = Mathf.Max(repScale, 0f);

Vector3 F_rep = normal * repScale;
Vector3 F_tan = tangent * repScale; // 切向也随距离衰减

F += F_rep * wRep + F_tan * wTan;
3️⃣ 缺少“离开墙体”的机制（第二大问题）

你现在是：

只要靠近障碍 → 一直 wall-following

👉 但正确逻辑应该是：

✅ 什么时候停止贴墙？

👉 当“可以直接朝目标走而不碰撞”

✔ 加一个检测：
bool canGoDirect = CheckLineFree(current.position, goal, shadowCollisionMonitor);

if (canGoDirect)
{
    wallFollowSign = 0; // 解除锁定
    F = F_att;          // 直接朝目标
}

👉 这个是从：

APF → Bug算法（Bug1/Bug2）

的关键一步

4️⃣ stuck机制还不够“聪明”

你现在：

if (Mathf.Abs(distDelta) < 1e-4f)

👉 问题：

只看“距离变化”
但绕路时本来就可能变远
✔ 更好的 stuck 判断：
if (Vector3.Distance(current.position, start.position) > totalStartDist * 2)
{
    // 说明在绕圈
}

或：

记录最近100步轨迹 → 判断是否在小范围震荡